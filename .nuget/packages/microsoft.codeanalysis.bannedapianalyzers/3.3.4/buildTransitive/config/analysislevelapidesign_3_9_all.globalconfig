# NOTE: Requires **VS2019 16.7** or later

# 'ApiDesign' Rules from '3.9' release with 'All' analysis mode
# Description: 'ApiDesign' Rules with enabled-by-default state from '3.9' release with 'All' analysis mode. Rules that are first released in a version later than '3.9' are disabled.

is_global = true

global_level = -99

